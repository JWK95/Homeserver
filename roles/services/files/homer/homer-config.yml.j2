---
title: "Pi Dashboard"
logo: "assets/favicon.ico"

header: false
footer: ""

defaults:
  layout: list
  colorTheme: dark

services:
  - name: "Services"
    icon: "fas fa-concierge-bell"
    items:
    {% for service in services %}

      - name: "{{ service }}"
        logo: "assets/{{ service | lower }}.png"
        url: "https://{{ service | lower }}.local"
 
    {% endfor %}